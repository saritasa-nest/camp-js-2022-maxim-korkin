/**
 * Information about pagination to enable/disable pagination buttons.
 */
export interface PaginationStatus {

  /** Shows if there is next page. */
  hasNext: boolean;

  /** Shows if there is prev page. */
  hasPrev: boolean;
}
