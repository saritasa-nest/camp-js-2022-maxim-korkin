/**
 * Interface describing sign in and sign up form fields.
 */
export interface FormFields {

  /** User's email. */
  readonly email: string;

  /** User's password. */
  readonly password: string;
}
